---
galaxy_info:
  role_name: videobridge
dependencies:
  - role: systemli.apt_repositories
    vars:
      apt_repositories:
        - preset: jitsi
          name: download_jitsi_org
          packages: "{{ jitsi_meet_apt_repositories_pin_packages }}"
