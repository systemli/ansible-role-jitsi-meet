---
dependencies:
  - role: systemli.apt_repositories
    vars:
      apt_repositories:
        - preset: jitsi
          name: download_jitsi_org
          packages:
            - jitsi-videobridge2
